version: '3.8'

services:
  vcore:
    build:
      context: .
      dockerfile: Dockerfile.local
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=mysql+pymysql://cdcuser:Divyam123@ls-71c57fcd322c32a3616fd3e00e212391e383d4ba.c7cuq02uskcx.ap-south-1.rds.amazonaws.com:3306/vcore
      - SECRET_KEY=vcore-glassy-secret-key-2026-change-in-production
      - ENVIRONMENT=development
    volumes:
      - ./templates:/app/templates
      - ./static:/app/static
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
